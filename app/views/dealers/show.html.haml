- if flash[:notice]
  .alert.alert-success= notice

%p
  %b Name:
  = @dealer.name

= button_to 'Edit', edit_dealer_path(@dealer), method: :get, class: 'btn btn-secondary'

= button_to 'Back', dealers_path, method: :get, class: 'btn btn-light'
