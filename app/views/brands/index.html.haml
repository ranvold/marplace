%h1 Brands

= button_to 'Add brand', new_brand_path, method: :get, class: 'btn btn-primary mb-2'

%table.table.table-bordered.table-striped
  %thead
    %tr
      %th Name
      %th Description
      %th
      %th
      %th

  %tbody.align-middle
    - @brands.each do |brand|
      %tr{"url" => "/brands/" + brand.id.to_s}
        %td.width_for_string_field
          = brand.name
        %td= brand.description
        %td.width_for_sed
          = button_to 'Show', brand, method: :get, class: 'btn btn-info'
        %td.width_for_sed
          = button_to 'Edit', edit_brand_path(brand), method: :get, class: 'btn btn-secondary'
        %td.width_for_sed
          %button.deleteAction.btn.btn-danger{:type => "button"} Delete