%p#notice.alert.alert-success= notice

%p
  %b Name:
  = @model.name
%p
  %b Brand:
  = @model.brand.name
%p
  %b Description:
  = @model.description

= button_to 'Edit', edit_model_path(@model), method: :get, class: 'btn btn-secondary'

= button_to 'Back', models_path, method: :get, class: 'btn btn-light'
